# Gerador de QR Code em Python

Usa o CustomTkinter para criar a GUI, usa os módulos QRCode, Pillow(pil), e o filedialog do Tkinter para gerar o QR Code instantaneamente e permite que o usuário salve o arquivo onde desejar.
para executar é necessário instalar os módulos: pip install qrcode[pil] customtkinter
